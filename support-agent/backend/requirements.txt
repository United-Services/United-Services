fastapi==0.115.6
uvicorn[standard]==0.34.0
# Bumped off the 0.3.x/0.2.x lines (0.3.14/0.3.1/0.2.62) to clear
# Dependabot alerts with no fix on those lines: LangGraph's checkpoint
# msgpack deserialization (fixed 1.0.10+), LangChain's file-search
# middleware/loaders path traversal (fixed 1.3.9+), and
# langchain-openai's image-token-counting SSRF-via-DNS-rebinding (fixed
# 1.1.14+). create_react_agent's system-prompt kwarg is now `prompt`,
# not `state_modifier` — see agent.py's own comment; verified against a
# fake deterministic model (astream_events tool_start/tool_end + final
# text) since live OpenRouter re-verification is quota-limited.
# create_react_agent itself is deprecated as of langgraph 1.0 in favor
# of langchain.agents.create_agent (removal target: langgraph 2.0, not
# yet released) — not migrated here since that's a materially different
# API, out of scope for a vulnerability-fix pass.
langchain==1.3.18
langchain-openai==1.6.0
langchain-text-splitters==1.1.2
langgraph==1.2.11
pydantic-settings==2.7.1
python-dotenv==1.2.2

# Phase 2 — search_knowledge_base needs to embed the *query* with the
# same model ingestion/embed.py used on the documents (see that file's
# module docstring on why this is duplicated, not imported, across the
# ingestion/backend Docker build-context boundary). requests, not
# sentence-transformers/torch — embeddings come from Hugging Face's
# hosted Inference API instead of a local model.
requests==2.33.0
qdrant-client==1.12.1

# Phase 3 — tickets + conversation sessions, persisted to Postgres.
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# Phase 4 — live conversation buffer. SSE itself is plain
# StreamingResponse + manual "data: ...\n\n" formatting, no extra
# dependency needed for that part.
redis==5.2.1

# /chat/stream now requires a signed-in visitor (app/security/clerk_auth.py)
# — verifies the same Clerk session token the main site's ClerkAuthGuard
# does, so the transcript can be keyed by real user id instead of a
# client-supplied session_id.
pyjwt[crypto]==2.13.0
