// Only these two carry a machine translation — 'en' (or anything else,
// including an omitted param) is the untranslated path with zero added
// logic. Kept in sync with routing.locales minus "en" in the frontend's
// i18n/routing.ts.
export const TRANSLATABLE_LOCALES = ['ar', 'zh'];
